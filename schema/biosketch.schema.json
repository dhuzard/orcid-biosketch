{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/dhuzard/orcid-biosketch/raw/main/schema/biosketch.schema.json",
  "title": "ORCID Biosketch",
  "type": "object",
  "required": ["schema_version", "person", "employment", "education", "works", "provenance"],
  "properties": {
    "schema_version": {"const": "0.2.0"},
    "person": {
      "type": "object",
      "required": ["name", "orcid", "orcid_url", "biography", "keywords", "urls"],
      "properties": {
        "name": {"type": "string"},
        "orcid": {"type": "string", "pattern": "^[0-9]{4}-[0-9]{4}-[0-9]{4}-[0-9]{3}[0-9X]$"},
        "orcid_url": {"type": "string", "format": "uri"},
        "biography": {"type": "string"},
        "keywords": {"type": "array", "items": {"type": "string"}},
        "urls": {"type": "object", "additionalProperties": {"type": "string"}}
      },
      "additionalProperties": true
    },
    "employment": {"type": "array"},
    "education": {"type": "array"},
    "works": {"type": "array"},
    "fundings": {"type": "array", "description": "Grants and awards, with funder, amount and grant identifiers."},
    "peer_reviews": {"type": "array", "description": "Peer review activity aggregated per convening organization."},
    "distinctions": {"type": "array"},
    "memberships": {"type": "array"},
    "services": {"type": "array"},
    "qualifications": {"type": "array"},
    "invited_positions": {"type": "array"},
    "research_resources": {"type": "array"},
    "selection": {
      "type": "object",
      "description": "Researcher-controlled output selection policy and annotations.",
      "additionalProperties": true
    },
    "provenance": {"type": "object", "required": ["primary_source", "generated_at"]}
  },
  "additionalProperties": false
}
