const template = document.createElement("template");
template.innerHTML = `<style>
:host{--ob-accent:#4b6f44;--ob-border:#d9dfd7;--ob-muted:#5d685b;--ob-bg:#fff;--ob-soft:#f5f7f4;display:block;color:#182019;font:15px/1.55 system-ui,sans-serif}*{box-sizing:border-box}article{border:1px solid var(--ob-border);border-radius:14px;background:var(--ob-bg);overflow:hidden}header,section{padding:1.4rem;border-bottom:1px solid var(--ob-border)}section:last-child{border:0}h2,h3{margin:0;line-height:1.2}h2{font:700 1.8rem Georgia,serif}h3{margin-bottom:.8rem;font:700 1.2rem Georgia,serif}p{margin:.7rem 0 0}a{color:var(--ob-accent)}.muted,.headline{color:var(--ob-muted)}.headline{font-weight:600}.actions{margin-top:1rem}button,input,select{border:1px solid var(--ob-border);border-radius:7px;background:var(--ob-bg);color:inherit;font:inherit;padding:.55rem .7rem}button{cursor:pointer}.filters{display:grid;grid-template-columns:2fr 1fr 1fr;gap:.7rem}.filters>*{width:100%}.table-wrap{margin-top:.7rem;overflow-x:auto;border:1px solid var(--ob-border);border-radius:9px}table{width:100%;min-width:680px;border-collapse:collapse;font-size:.9rem}th,td{padding:.65rem;border-bottom:1px solid var(--ob-border);text-align:left;vertical-align:top}th{background:var(--ob-soft);color:var(--ob-muted);font-size:.75rem;text-transform:uppercase}.title{font-weight:650}.count,.provenance{color:var(--ob-muted);font-size:.82rem}.provenance{background:var(--ob-soft)}.error{border:1px solid #d99;padding:1rem;color:#7b2525}@media(max-width:700px){.filters{grid-template-columns:1fr}}
</style><div class="root" aria-live="polite"><p class="muted">Loading current ORCID biosketch…</p></div>`;

class OrcidBiosketch extends HTMLElement {
  constructor(){super();this.attachShadow({mode:"open"}).append(template.content.cloneNode(true));this.bio=null}
  static get observedAttributes(){return["src","works","search","pdf","provenance","bio"]}
  attributeChangedCallback(){if(this.isConnected)this.load()}
  connectedCallback(){this.load()}
  enabled(name,fallback=false){return this.hasAttribute(name)?this.getAttribute(name)!=="false":fallback}
  el(tag,text,className){const node=document.createElement(tag);if(className)node.className=className;if(text!=null)node.textContent=text;return node}
  async load(){const src=this.getAttribute("src");if(!src)return this.error("Missing required src attribute.");try{const r=await fetch(src,{cache:"no-store"});if(!r.ok)throw new Error(`HTTP ${r.status}`);const data=await r.json();if(!data?.person?.orcid||!Array.isArray(data.works))throw new Error("Invalid biosketch contract");this.bio=data;this.render()}catch(e){this.error(`Could not load biosketch: ${e.message}`)}}
  render(){const b=this.bio,p=b.person,root=this.shadowRoot.querySelector(".root");root.replaceChildren();const article=this.el("article"),head=this.el("header");head.append(this.el("h2",p.name||"Researcher"));if(p.headline)head.append(this.el("p",p.headline,"headline"));const orcid=this.el("a",`ORCID ${p.orcid}`);orcid.href=p.orcid_url;orcid.rel="me noopener";head.append(orcid);if(this.enabled("pdf")){const actions=this.el("div",null,"actions"),button=this.el("button","Export PDF");button.type="button";button.onclick=()=>this.exportPdf();actions.append(button);head.append(actions)}article.append(head);if(this.enabled("bio",true)&&p.biography){const section=this.el("section");section.append(this.el("h3","Profile"));p.biography.split(/\n\s*\n/).filter(Boolean).forEach(x=>section.append(this.el("p",x)));article.append(section)}article.append(this.renderWorks());if(this.enabled("provenance",true)){const section=this.el("section",null,"provenance"),updated=b.provenance?.generated_at?new Date(b.provenance.generated_at).toLocaleString():"not reported";section.textContent=`Source: ORCID ${p.orcid}. Record updated ${updated}. Schema ${b.schema_version||"unknown"}.`;article.append(section)}root.append(article)}
  renderWorks(){const section=this.el("section");section.append(this.el("h3","Publications and research outputs"));const works=[...this.bio.works],configured=this.getAttribute("works")||"10",limit=configured==="all"?Infinity:Math.max(0,Number(configured)||10);let controls=null;if(this.enabled("search")){const filters=this.el("div",null,"filters"),search=this.el("input"),type=this.el("select"),sort=this.el("select");search.type="search";search.placeholder="Search title, journal, DOI, year…";search.setAttribute("aria-label","Search outputs");type.append(new Option("All types",""));[...new Set(works.map(x=>x.type).filter(Boolean))].sort().forEach(x=>type.append(new Option(x.replaceAll("-"," "),x)));sort.append(new Option("Newest first","new"),new Option("Oldest first","old"),new Option("Title A-Z","title"));filters.append(search,type,sort);section.append(filters);controls={search,type,sort}}
    const count=this.el("p",null,"count"),wrap=this.el("div",null,"table-wrap"),table=this.el("table"),thead=this.el("thead"),hr=this.el("tr"),body=this.el("tbody");["Year","Title","Venue","Type","Record"].forEach(x=>hr.append(this.el("th",x)));thead.append(hr);table.append(thead,body);wrap.append(table);section.append(count,wrap);
    const draw=()=>{let items=works.filter(w=>{if(!controls)return true;if(controls.type.value&&w.type!==controls.type.value)return false;const q=controls.search.value.trim().toLowerCase(),text=[w.title,w.journal,w.type,w.publication_date,...Object.values(w.identifiers||{})].join(" ").toLowerCase();return!q||text.includes(q)});items.sort((a,b)=>controls?.sort.value==="title"?String(a.title||"").localeCompare(String(b.title||"")):(controls?.sort.value==="old"?1:-1)*String(a.publication_date||"").localeCompare(String(b.publication_date||"")));const visible=items.slice(0,limit);body.replaceChildren(...visible.map(w=>this.workRow(w)));count.textContent=`${visible.length} shown of ${items.length} matching outputs (${works.length} total)`};if(controls)Object.values(controls).forEach(x=>{x.oninput=draw;x.onchange=draw});draw();return section}
  workRow(w){const row=this.el("tr");[String(w.publication_date||"").slice(0,4)||"—",w.title||"Untitled",w.journal||"—",String(w.type||"—").replaceAll("-"," ")].forEach((x,i)=>row.append(this.el("td",x,i===1?"title":"")));const cell=this.el("td"),doi=w.identifiers?.doi,url=doi?`https://doi.org/${doi}`:w.url;if(url){const a=this.el("a",doi?"DOI ↗":"Open ↗");a.href=url;a.target="_blank";a.rel="noopener";cell.append(a)}else cell.textContent="—";row.append(cell);return row}
  exportPdf(){const b=this.bio,e=v=>String(v||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]),entries=xs=>(xs||[]).map(x=>`<div class="entry"><b>${e([x.role,x.organization].filter(Boolean).join(", "))}</b><small>${e([x.department,x.start_date,x.end_date||"present"].filter(Boolean).join(" | "))}</small></div>`).join(""),works=[...b.works].sort((a,z)=>String(z.publication_date||"").localeCompare(String(a.publication_date||""))).map(w=>`<div class="work"><span>${e(String(w.publication_date||"").slice(0,4))}</span><div><b>${e(w.title)}</b><small>${e([w.journal,String(w.type||"").replaceAll("-"," "),w.identifiers?.doi?`doi:${w.identifiers.doi}`:""].filter(Boolean).join(" | "))}</small></div></div>`).join(""),paragraphs=e(b.person.biography).split(/\n\s*\n/).map(x=>`<p>${x}</p>`).join(""),popup=window.open("","_blank");if(!popup)return this.error("Allow pop-ups to export the PDF.");popup.opener=null;popup.document.write(`<!doctype html><title>${e(b.person.name)} - ORCID biosketch</title><style>@page{size:A4;margin:15mm}body{font:9.5pt/1.42 Arial;color:#17202a}h1{font:24pt Georgia;margin:0}h2{font:14pt Georgia;border-bottom:1px solid #ccd3da;padding-bottom:4px;margin-top:22px}.head{border-bottom:2px solid #243447;padding-bottom:16px}.muted,small{display:block;color:#607080}.entry,.work{break-inside:avoid;margin:8px 0}.work{display:grid;grid-template-columns:40px 1fr;gap:10px;border-bottom:1px solid #e1e5e9;padding:6px 0}</style><header class="head"><h1>${e(b.person.name)}</h1><p>${e(b.person.headline)}</p><span class="muted">ORCID ${e(b.person.orcid)}</span></header><h2>Professional profile</h2>${paragraphs}${b.employment?.length?`<h2>Employment</h2>${entries(b.employment)}`:""}${b.education?.length?`<h2>Education</h2>${entries(b.education)}`:""}<h2>Research outputs</h2><p class="muted">${b.works.length} public outputs, newest first.</p>${works}<p class="muted">Generated from ORCID via orcid-biosketch. Record updated ${e(b.provenance?.generated_at)}.</p><script>window.onload=()=>window.print()<\/script>`);popup.document.close()}
  error(message){const root=this.shadowRoot.querySelector(".root");root.replaceChildren(this.el("p",message,"error"))}
}
if(!customElements.get("orcid-biosketch"))customElements.define("orcid-biosketch",OrcidBiosketch);
